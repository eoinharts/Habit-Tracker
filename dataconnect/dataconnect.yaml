specVersion: "v1beta"
serviceId: "momentum-data-connect"
location: "europe-west2"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "momentum-be"
      cloudSql:
        instanceId: "momentum-sql"
      # schemaValidation: "COMPATIBLE"
allowedOrigins:
  - http://localhost:5173
connectorDirs: ["./connector"]